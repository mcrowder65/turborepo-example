{
  "name": "rs",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "shared": "*"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "yarn start",
    "build": "react-scripts build",
    "test": "jest --config ../../jest.config.js rs/**/*.js",
    "eject": "react-scripts eject",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
